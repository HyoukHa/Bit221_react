/**
 * Virtual DOM --------------------- render --------------------- Browser Dom
 *   jsx                                                             html
 *
 * elements 특징
 * 불변성 immutable
 * 한번 생성된 엘리먼트는 변화하지 않는다.
 * 즉, 생성된 후에는 속성 등 변경 불가.
 *
 * 따라서 엘리먼트를 변경하기 위해서는 다시 생성해야 한다.
 */
